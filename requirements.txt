# Core runtime (NER, retrieval, KB build, scoring).
# Ranges chosen for compatibility; adjust pins if you need stricter reproducibility.
torch>=2.4,<3.0
transformers>=5.0,<6.0
faiss-cpu>=1.7,<2.0

# Data / config
numpy>=1.26,<3
pandas>=2.0,<3
pyarrow>=15,<17
PyYAML>=6.0,<7
openpyxl>=3.1

# Env / tooling
python-dotenv>=1.0
huggingface-hub>=0.16.4
tqdm>=4.65

rank-bm25